### 버전관리
- 안정버전(기본값): curl -fsSL https://claude.ai/install.sh | bash
- 최신 기능: curl -fsSL https://claude.ai/install.sh | bash -s latest
- 특정 버전: curl -fsSL https://claude.ai/install.sh | bash -s 2.0.75

###  자동업데이트 비활성화
```
$ export DISABLE_AUTOUPDATER=1
```

### 설정 디렉토리
```
project-root/
├── .cluade/
│   ├── commands/             # 커스텀 슬래시 명령어
│   ├── agents/               # 커스텀 에이전트 정의
│   ├── skills/               # 에이전트 스킬 모듈
│   ├── hooks/                # 이벤트 훅 스크립트
│   ├── output-styles/        # 출력 스타일 템플릿
│   └── setting.json          # 프로젝트별 클로드 코드 설정
└── CLAUDE.md                 # 프로젝트 지침서(컨텍스트로 자동 로드)
```

### 실행방법
```
- 일회성 작업: claude "질문"
- 비대화형 쿼리: claude -p
- 최근대화 세션 재개: claude -c
- 이전 대화 세션 중 선택 재개: claude -r
```

### 명령어
- /clear: 현재 대화 컨텍스트 초기화. 프로젝트 설정과 체크포인트 유지 되므로 /rewind로 이전 상태 복구 가능
- claude doctor: 정상 설치 여부 확인. 설치 유형, 경로, 상태 등을 진단하여 출력

### checkpointing
- 파일 편집을 자동으로 추적하므로 빠르게 실행 취고하소 문제가 발생한 경우 이전 상태로 되돌릴 수 있음.
- `/rewind` 실행하거나 프롬프트 입력이 비어 있을 때 `Esc`를 두 번 눌러 rewind 메뉴를 연다.
- 프롬프트가 있을 때 Esc를 두번 누르면 메뉴를 열지 않고 텍스트를 지움
```
#rewind 메뉴
- 코드 및 대화 복원: 코드와 대화를 해당 지점으로 되돌림
- 대화복원: 현재 코드를 유지하면서 해당 메세지로 되돌림
- 코드 복원: 대화를 유지하면서 파일 변경 사항을 되돌림
- 여기서부터 요약
- 여기까지 요약
- 취소

```
- 유용한 상황
1. 시작점을 잃지 ㅇ나혹 대체 구현을 탐색해야 할 때
2. 잘못된 변경으로 버그가 생겼거나 기능이 깨져서 빠르게 복구해야 할 떄
3. 작동하는 현 상태를 유지하면서 실험해야 할 때
- 한계
```
1. Bash를 통한 파일 변경은 추적하지 못하므로, 전용 편집 도구를 사용하도록 유도하는 편이 안전
2. 외부 변경사항은 기록되지 않음. 별도 편집기에서 직접 수정, 다른 세션 등
3. 버전 관리 시스템의 대체재가 아님.

```

### 깃 통합
- 내부적으로 git status 와 git diff 를 실행항 변경 내용을 요약하고 사용자에게 보고

### 탐색과 분석
- 컨테스트 낭비 줄이기
1. 파일 찾기(A기능 파일 찾아줘)
2. 동작 설명(식별된 파일만 Read)
- 탐색 시 의도치 않은 변경을 차단하는 것이 중요하므로 `plan`모드를 활용하자
```
#plan mode 활성화
- 세션 중 변환: shift + tab
- 새 세션 시작: claude --permission-mode plan
- 비 대화형 모드: claude -permission-mode plan -p "기능을 분석하고 개선 방안을 제안"
```

### 점진적 개발 전략과 TDD
- 기능을 작은 단위로 분해 + 지속적 검증 = TDD
- TDD Red-Gree-Refactor Cycle 
- 실패하는 테스트 작성 -> 테스트를 통과하는 최소 코드 구현 -> 코드 구조 개선
- 테스트 결과를 다음 행동을 결정하는 근거로 활용
1. 테스트를 작성하고 
2. 테스트가 실패하는지 확인한 뒤
3. 테스트를 통과하도록 코드를 구현해줘. 
- 라고 지시하면 통과할 떄 까지 코드를 수정
- refactor 단계에서는 통과된 코드의 기능을 유지하면서 구조를 개선 또는 중복 제거

### npm 스크립트로 AI 코드 검토 자동화
```
{
    "scripts": {
        "lint": "eslint src/",
        "lint:claude": "claude -p '당신은 코드 리뷰어다. main 브랜치와 비교하여 변경된 코드를 검토하고 오타 및 잠재적 버그를 찾으시오.'"
    }
}
```
